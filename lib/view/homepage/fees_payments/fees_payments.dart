import 'package:flutter/material.dart';

class FeesPayments extends StatelessWidget {
  const FeesPayments({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("comming soon..."),
    );
  }
}
